# Fork Extensions

Place fork-specific, additive features here when possible.

Guidelines:

- prefer new files/packages over edits to upstream files
- avoid changing upstream interfaces unless unavoidable
- keep extension entry points explicit and small

Related:

- `fork/extensions/AGENT_LEARNINGS.md` stores fork-local agent guidance and session learnings.
